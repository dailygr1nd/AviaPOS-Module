def create_transfer(*args, **kwargs):
    raise RuntimeError(
        "modules.transfers.service.create_transfer is deprecated. "
        "Use modules.transfers.api with transfer commands instead."
    )